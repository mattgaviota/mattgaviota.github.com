{
  "name": "personal-web",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "nuxt": "^1.0.0-alpha.4"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
